
def get_matrix(n, m, value):
    matrix = []
    for i in range(n):
        matrix.append([])   # добавляем список [2]
    for j in range(m):
        matrix[i].append(value) # пишем в список значение [2]
    print(matrix)
